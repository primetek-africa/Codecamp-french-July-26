# React + Vite
# Prérequis à vérifier avant de commencer  :
dans le terminal bash
node -v
npm -v

"Si ces commandes affichent des numéros de version, Node.js est prêt. Sinon, direction nodejs.org pour l'installer  même logique que pour Git il y a quelques semaines."

# pour Créer un nouveau projet  react:
npm create vite@latest mon-projet-react
[23:58, 21/08/2026] Eray: bash
cd mon-projet-react
- npm install
- npm run dev

# "npm install:
 télécharge toutes les dépendances nécessaires (ça peut prendre une minute).

# npm run dev 
 démarre un serveur de développement local  regardez le terminal, il vous donne une adresse du type http://localhost:5173, à ouvrir dans le navigateur."
